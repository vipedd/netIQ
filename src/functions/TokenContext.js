import { createContext } from 'react';

// Creation of the TokenContext Hook, to share the access token along all the components
const TokenContext =  createContext();

export default TokenContext;
